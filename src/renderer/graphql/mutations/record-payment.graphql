mutation RecordPayment($id: ID!, $input: RecordPaymentInput!) {
  recordPayment(id: $id, input: $input) {
    invoice {
      _id
      paid_amount_usd
      payment_status
    }
    error {
      field
      message
    }
  }
}
