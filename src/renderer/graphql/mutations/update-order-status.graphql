mutation UpdateOrderStatus($id: ID!, $input: UpdateOrderStatusInput!) {
  updateOrderStatus(id: $id, input: $input) {
    order {
      _id
      status
    }
    error {
      field
      message
    }
  }
}
