mutation CreateProduct($input: CreateProductInput!) {
  createProduct(input: $input) {
    product {
      _id
      sku
      name
      category
      price_usd
      quantity_in_stock
      reorder_level
      status
      barcode
      description
    }
    error {
      field
      message
    }
  }
}
