query GetCustomers($filter: CustomerFilterInput) {
  customers(filter: $filter) {
    customers {
      _id
      name
      email
      phone
      addresses {
        type
        street
        city
        state
        postal_code
        country
        is_default
      }
      notes
    }
    error {
      field
      message
    }
    length
    page
    limit
  }
}
