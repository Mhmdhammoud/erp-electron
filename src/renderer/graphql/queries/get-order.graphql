query GetOrder($id: ID!) {
  order(id: $id) {
    order {
      _id
      customer_id
      order_number
      status
      currency_used
      exchange_rate_used
      total_usd
      total_lbp
      items {
        product_id
        product_name
        product_sku
        quantity
        unit_price_usd
        subtotal_usd
      }
      notes
    }
    error {
      field
      message
    }
  }
}
